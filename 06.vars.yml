---
- name: play level variables
  hosts: local
  connection: local
  vars_file:
   - course.yml
  tasks:
   - name: Print play level variables
     vars: 
       Course: Ansible
     ansible.builtin.debug:
        msg: I will complete {{ Course }} taught by {{ Tutor }} in {{ Days }} Days
